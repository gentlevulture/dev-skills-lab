<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dev Skills Lab</title>
  <link rel="stylesheet" href="stylesheets/style.css">
</head>
<body>
  <h1>Dev Skills To Develop as of <%= time %> </h1>
  <% if(error) { %>
    <h2><%= error %></h2>
  <% } %>
  <ul>
    <% todos.forEach(function(todo) { %>
      <li>
        <form 
        action="/todos/<%= todo._id %>?_method=DELETE"
        class="delete-form"
        method="POST"
      >
        <button type="submit">X</button>
      </form>
        <a href="/todos/<%= todo._id %>"><%= todo.text %></a>
          - 
        <%= todo.done ? 'developed' : 'not developed' %>
      </li>
    <% }) %>
  </ul>
  <a href="/todos/new">Add A Skill To Learn</a>
</body>
</html>